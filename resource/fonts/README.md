# Fonts

Font binaries are not committed to this repository.

Use `system-default` to select an installed Unicode-capable system font, or place a
licensed `.ttf`/`.ttc` file in this directory and set `font_name` to its filename.
The Docker image installs Noto CJK through the operating system package manager.
